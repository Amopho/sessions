import "./App.css";

function App() {
  return (
    <div className="App">
      {/* useRef-  short from reference, I grab a variable and may reference it in other objectt is not similar with useState but they share some common things. It may be a replacement for ID in DOM. WHenever you will use an useRef it will come back with an object. A lot of properties- current- on which projwct you are currently in*/}
    </div>
  );
}

export default App;
